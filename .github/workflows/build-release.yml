name: Build Release Packages
on: [push]
jobs:
  build-ubuntu-x86-64:
    runs-on: ubuntu-20.04
    steps:
      - run: |
          echo "Hello World"
          ls -la
          ls -la ..
          ls -la ../bladebit
          pwd